apiVersion: v1
kind: Service
metadata:
  name: mcp-demo
  labels:
    app.kubernetes.io/component: mcp-demo
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/component: mcp-demo
